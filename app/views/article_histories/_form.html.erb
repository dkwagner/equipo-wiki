<%= form_with(model: article_history, local: true) do |form| %>
  <% if article_history.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(article_history.errors.count, "error") %> prohibited this article_history from being saved:</h2>

      <ul>
      <% article_history.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :article_id %>
    <%= form.text_field :article_id %>
  </div>

  <div class="field">
    <%= form.label :title %>
    <%= form.text_field :title %>
  </div>

  <div class="field">
    <%= form.label :body %>
    <%= form.text_area :body %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
